[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aura-core-prototype"
version = "0.1.0"
description = "A.U.R.A. core prototype"
requires-python = ">=3.9"

dependencies = [
  "pyyaml",
  "click"
]

[project.scripts]
aura = "aura.cli:main"
